BASIC_PKG_INFO

This package contains the ABI versioned files of kmod version PKG_UPSTREAM_VERSION.
The documentation and the development files are contained in kmod-dev, and the
non ABI versioned files in kmod.

LICENSE_LGPL21([kmod])

ERBTH_FOOTER
