BASIC_PKG_INFO

This package contains the libraries of flex version PKG_UPSTREAM_VERSION.
Their ABI versions are not respected that is no ABI versioned packages exist.

Licensing information if provided by the upstream package in an easily readable
manner, can be found in /usr/share/doc/PKG_NAME.
It is probably incomplete for now and may stay incomplete forever.

ERBTH_FOOTER
