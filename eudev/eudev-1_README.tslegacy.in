BASIC_PKG_INFO

This package contains the ABI versioned libudev, which is part of eudev
version PKG_UPSTREAM_VERSION.
The nun ABI versioned runtime files are contained in eudev-<ABI>, and the
development files and the documentation is contained in eudev-dev.

LICENSE_GPL20([Eudev])

ERBTH_FOOTER
