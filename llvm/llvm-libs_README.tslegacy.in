BASIC_PKG_INFO

This package contains the libraries of LLVM version PKG_UPSTREAM_VERSION.
Their ABI versions are not respected that is no ABI versioned packages exist.

Licensing information can be found in /usr/share/doc/PKG_NAME, it is probably
incomplete.

ERBTH_FOOTER
