ifndef skel_description_included
skel_description_included := 1

include ${PACKAGING_RESOURCE_DIR}/makefile_utilities.mk

include $(PACKAGING_RESOURCE_DIR)/util-macros/description.mk

skel_SRC_VERSION := skel_version
skel_SRC_DIR := skel-$(skel_SRC_VERSION)
skel_SRC_ARCHIVE := $(skel_SRC_DIR).tar.skel_compression
skel_SRC_CDEPS := \
	glibc-dev_installed \
	gcc_installed \
	basic_fhs-dev_installed \
	util-macros-dev_installed

skel-dev_TSL_TYPE := sw
skel-dev_TSL_RDEPS = \
	$(call bigger_equal_dep,util-macros)
skel-dev_TSL_SRC_PKG := skel

skel_TSL_PKGS := skel-dev

endif
