BASIC_PKG_INFO

This package contains the libraries and other runtime files of libxcb
version PKG_UPSTREAM_VERSION. Their ABI version is not respected that is no ABI
versioned packages exist.

Licensing information (if provided by the source package in an easily readable
manner) can be found in /usr/share/doc/PKG_NAME.

ERBTH_FOOTER
